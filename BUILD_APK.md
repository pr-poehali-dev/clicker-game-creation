# 📱 Инструкция: Как собрать APK для Android

Твоя игра **Financial Empire** готова к превращению в Android-приложение! Следуй этим шагам:

---

## 🛠 Что нужно установить

### 1. Android Studio
- Скачай и установи [Android Studio](https://developer.android.com/studio)
- Во время установки выбери "Standard" setup
- После установки открой Android Studio и перейди в **Tools → SDK Manager**
- Убедись, что установлены:
  - ✅ Android SDK Platform (API 34 или новее)
  - ✅ Android SDK Build-Tools
  - ✅ Android SDK Command-line Tools

### 2. Java Development Kit (JDK)
- Android Studio обычно устанавливает JDK автоматически
- Проверь наличие: открой терминал и введи `java -version`
- Должна быть версия JDK 17 или новее

---

## 🚀 Пошаговая сборка APK

### Шаг 1: Подключи GitHub
```bash
# В редакторе poehali.dev нажми:
# Скачать → Подключить GitHub
# После подключения склонируй репозиторий на свой компьютер
```

### Шаг 2: Собери веб-версию
```bash
# В терминале в папке проекта выполни:
npm install
npm run build
```

### Шаг 3: Добавь Android платформу
```bash
# Инициализируй Capacitor (только первый раз)
npx cap init "Financial Empire" "dev.poehali.financialempire"

# Добавь Android платформу
npx cap add android

# Скопируй веб-билд в Android проект
npx cap sync
```

### Шаг 4: Открой проект в Android Studio
```bash
# Открой Android проект
npx cap open android
```

### Шаг 5: Собери APK в Android Studio
1. **Подожди**, пока Android Studio завершит индексацию и синхронизацию Gradle
2. В меню выбери: **Build → Build Bundle(s) / APK(s) → Build APK(s)**
3. Дождись завершения сборки (появится уведомление)
4. Нажми **locate** в уведомлении или найди APK в:
   ```
   android/app/build/outputs/apk/debug/app-debug.apk
   ```

---

## 📲 Установка на телефон

### Вариант 1: Прямая установка
1. Скопируй `app-debug.apk` на телефон (через USB, облако или мессенджер)
2. Открой файл на телефоне
3. Разреши установку из неизвестных источников (если попросит)
4. Установи приложение

### Вариант 2: Через ADB
```bash
# Подключи телефон по USB с включенной отладкой
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 🎨 Кастомизация приложения

### Изменить название приложения
Отредактируй `android/app/src/main/res/values/strings.xml`:
```xml
<string name="app_name">Financial Empire</string>
```

### Изменить иконку приложения
Замени файлы в `android/app/src/main/res/`:
- `mipmap-hdpi/ic_launcher.png` (72x72)
- `mipmap-mdpi/ic_launcher.png` (48x48)
- `mipmap-xhdpi/ic_launcher.png` (96x96)
- `mipmap-xxhdpi/ic_launcher.png` (144x144)
- `mipmap-xxxhdpi/ic_launcher.png` (192x192)

Используй [Icon Generator](https://romannurik.github.io/AndroidAssetStudio/icons-launcher.html)

### Изменить цвет темы
Отредактируй `android/app/src/main/res/values/styles.xml`

---

## 🔧 Обновление приложения после изменений в коде

```bash
# 1. Собери новую веб-версию
npm run build

# 2. Синхронизируй с Android
npx cap sync

# 3. Пересобери APK в Android Studio
# Build → Build Bundle(s) / APK(s) → Build APK(s)
```

---

## ⚠️ Частые проблемы

### Gradle sync failed
**Решение**: В Android Studio:
- File → Invalidate Caches → Invalidate and Restart

### JAVA_HOME not set
**Решение**: Добавь в переменные окружения:
```bash
# Windows (в системных переменных)
JAVA_HOME=C:\Program Files\Android\Android Studio\jbr

# macOS/Linux (в ~/.bashrc или ~/.zshrc)
export JAVA_HOME=/Applications/Android\ Studio.app/Contents/jbr/Contents/Home
```

### Cannot resolve symbol '@capacitor/core'
**Решение**:
```bash
npx cap sync android
```

---

## 📦 Release APK (для публикации)

Для создания подписанного APK для Google Play:

```bash
# 1. Создай ключ подписи (только первый раз)
keytool -genkey -v -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000

# 2. В Android Studio:
# Build → Generate Signed Bundle / APK → APK
# Следуй инструкциям мастера
```

---

## 🎮 Готово!

Теперь твоя игра работает как полноценное Android-приложение! 

**Что работает:**
- ✅ Все функции игры
- ✅ Сохранение прогресса
- ✅ Работает офлайн
- ✅ Нативная производительность
- ✅ Иконка на главном экране

**Следующие шаги:**
- Добавь кастомную иконку
- Настрой splash screen
- Опубликуй в Google Play Store

---

💡 **Поддержка**: Если что-то не работает, проверь версии:
```bash
npx cap doctor
```
